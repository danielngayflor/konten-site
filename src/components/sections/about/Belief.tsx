export default function Belief() {
  return (
    <section className="bg-konten-black text-white py-32 md:py-40 px-6 md:px-12">
      <div className="max-w-5xl mx-auto text-center">
        <p className="text-eyebrow text-mid-gray mb-12">We're driven by a clear belief.</p>
        <blockquote
          className="font-display leading-[1.1] tracking-tight text-white"
          style={{ fontSize: 'clamp(2.2rem, 5.5vw, 6rem)' }}
        >
          Stories real ones, told well have the power to move people to
          action.
        </blockquote>
      </div>
    </section>
  );
}
